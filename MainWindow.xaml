<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="app_test.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:app_test"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:controls="using:CommunityToolkit.WinUI.Controls" 
    mc:Ignorable="d"
    Title="app-test">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

        <NavigationView
            x:Name="NavView"
            PaneDisplayMode="Top"
            SelectionChanged="NavigationView_SelectionChanged"
            BackRequested="navigateBackButton_Click"
            IsBackButtonVisible="Auto">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Projets" Tag="Projets"/>
                <NavigationViewItem Content="Paramètres" Tag="Parametres"/>
                <NavigationViewItem Content="On verra" Tag="onVerra"/>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>

</Window>
